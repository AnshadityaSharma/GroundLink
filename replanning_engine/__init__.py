# Not implemented yet — this is the core novelty piece (see context.md) and
# will be built after the approach is reviewed. See decisions.md and the
# Step 4 report for what it needs to consume: mission_planner.Mission /
# Waypoint and constraint_monitor.ViolationEvent, both already stable.
