# Streamlit dashboard — not built yet (out of scope for the Step 1-4
# skeleton). Will consume constraint_monitor.ViolationEvent and
# firmware_link.TelemetrySnapshot streams once the replanning_engine
# approach is reviewed and built.
